# OpenCode Workflow State

**Project**: Optium
**Task**: Cleanup legacy audit findings — parser utilities, universal criteria naming, threshold validation, lint/static checks, migration planning
**Status**: Complete

## Planner-gpt
**Status**: complete
**Phase**: clarify

### Summary
Plan a cleanup pass that removes/renames safe runtime dead code, consolidates threshold validation comments/flow, and adds real static checks. **Do not remove DB columns in this pass**: `Metric.higher_is_better` and nullable `Activity.decision_id` are schema/API-breaking and should be handled by a later migration-backed change.

### Impacted files
- `services/parser.py`
- `services/ontology.py`
- `routers/api.py`
- `services/scoring.py`
- `models.py`
- `main.py`
- `tests/test_parser.py`
- `tests/test_ontology.py`
- `tests/test_routes.py`
- `frontend/src/types/index.ts`
- `frontend/src/components/*` or `frontend/src/lib/*` only if TypeScript still references removed compatibility fields
- `requirements-dev.txt`
- `pyproject.toml` if adding Ruff config
- `frontend/package.json`
- `.gitignore`

### Implementation plan

#### Safe now
1. **Legacy threshold parser utilities**
   - In `services/parser.py`, remove `extract_thresholds()` and `extract_thresholds_detailed()` from production code if tests confirm no runtime dependency.
   - Update parser tests:
     - Delete tests that only verify these legacy helpers, or
     - Move helper-only coverage into a test-local fixture/helper if preserving historical behavior is useful.
   - Keep runtime threshold behavior covered through `/api/decisions/{id}/thresholds` route tests and `services/scoring.py`.

2. **Rename misleading criteria function**
   - In `services/ontology.py`, rename `suggest_criteria(query)` to `get_universal_criteria()`.
   - Update imports/usages in `services/parser.py` and tests.
   - Since this is internal-only, avoid keeping a long-term alias unless tests reveal external dependency.

3. **Threshold validation consolidation**
   - Keep `routers/api.py::apply_thresholds()` as the primary request validation boundary.
   - Keep `services/scoring.py::sanitize_persisted_thresholds()` as the DB-corruption/backward-compatibility safety boundary.
   - Remove or simplify redundant clamping inside `filter_by_thresholds()` because sanitized thresholds should already be valid.
   - Add comments documenting the two intentional validation layers.

4. **Agent state file handling**
   - `.opencode-workflow-state.md` is already ignored; keep it out of product docs.
   - Do not rely on agent notes as lint/static checks.
   - Add real checks:
     - Backend: add Ruff to `requirements-dev.txt` and configure in `pyproject.toml`.
     - Frontend: add a `typecheck` script using `tsc --noEmit` or equivalent project-safe command.

#### Migration/deferred
5. **Do not remove `Metric.higher_is_better` now**
   - It is present in DB model, metric seed data, API responses, frontend types, and tests.
   - Defer to a dedicated migration task:
     - Add DB migration tooling or migration script.
     - Remove column from `models.py`.
     - Remove from `main.py` seed sync.
     - Remove from `/api/metrics`, decision detail/refine responses.
     - Remove from frontend types/usages.
     - Update tests and fixtures.

6. **Do not make `Activity.decision_id` non-nullable now**
   - This is DB/schema-breaking and may fail existing SQLite data.
   - Defer to migration task:
     - Audit for `activities.decision_id IS NULL`.
     - Decide whether to delete/archive global activities.
     - Add `ondelete="CASCADE"` to `ForeignKey("decisions.id", ondelete="CASCADE")`.
     - Make `nullable=False`.
     - Verify delete behavior and update route tests.

### Validation commands
- `pytest tests/ -q`
- `cd frontend && npm test`
- `cd frontend && npx tsc --noEmit`
- `cd frontend && npm run build`
- If Ruff is added: `ruff check .`

### Risks / deferred items
- Removing `higher_is_better` or changing `Activity.decision_id` nullability without a migration risks breaking existing `optium.db`, API clients, frontend types, and tests.
- Removing legacy parser utilities may require deleting obsolete tests rather than moving product logic into test helpers.
- Adding lint may surface unrelated existing style issues; keep config conservative for the first pass.

**Next agent**: debater-gpt

## Debater-gpt
**Status**: approved
**Phase**: review-plan
**Critique**: The plan is safe and appropriately scoped. It correctly defers DB-breaking changes (`Metric.higher_is_better`, `Activity.decision_id` nullability) to a migration-backed task. Deleting the legacy threshold parser helpers is safe if implementation confirms current usage remains test-only and route/scoring threshold coverage is preserved. Adding Ruff/config now is acceptable as a dev-only/static-check improvement, provided the config is conservative, pinned in `requirements-dev.txt`, and does not trigger broad unrelated refactors. Threshold validation layering is sound; keep route validation strict and persisted-data sanitization defensive. When simplifying `filter_by_thresholds()`, only remove redundant threshold clamping after sanitization—do not remove score clamping for potentially corrupt persisted `AlternativeScore` data.
**Required changes**: None
**Next agent**: implementer-gpt

## Implementer-gpt
**Status**: complete
**Phase**: implement
**Changes made**:
- `models.py`: Removed `Metric.higher_is_better`; made `Activity.decision_id` non-null with `ondelete="CASCADE"` while keeping existing ORM delete-orphan ownership.
- `services/ontology.py`, `main.py`: Removed metric direction seed data and seed synchronization.
- `routers/api.py`: Removed public `mode`, `category`, and `higher_is_better` from decide/list/detail/refine/metrics JSON responses; kept `Decision.mode` internal for diagnose branching; reworded threshold persisted-data guard comment.
- `services/export.py`: Removed `mode` and `category` from export data and markdown output.
- `frontend/src/types/index.ts`: Public response types now omit removed fields.
- `frontend/src/lib/api.ts`: Validated API helpers; no compatibility mapping or removed-field preservation was present, so no file change was needed.
- `frontend/src/lib/scoring.ts`: Removed old score-row compatibility fallback, made current `metric_id` shape required, and removed compatibility `as any` usage.
- `frontend/src/components/Landing.tsx`, `frontend/src/components/DecisionList.tsx`, `frontend/src/components/Results.tsx`: Removed raw mode/category UI usage and updated sensitivity recomputation call.
- `tests/*`, `frontend/tests/scoring.test.ts`: Updated fixtures/assertions for removed fields, current metric shape, public response contract, and export output.
- `AGENTS.md`, `README.md`, `VISION.md`: Removed stale legacy/public mode/schema wording and documented local DB reset expectations for breaking dev schema changes.
**Files created**: None
**Files modified**:
- `AGENTS.md`
- `README.md`
- `VISION.md`
- `models.py`
- `main.py`
- `routers/api.py`
- `services/export.py`
- `services/ontology.py`
- `tests/test_ontology.py`
- `tests/test_robustness.py`
- `tests/test_routes.py`
- `tests/test_scoring.py`
- `frontend/src/types/index.ts`
- `frontend/src/lib/scoring.ts`
- `frontend/src/components/Landing.tsx`
- `frontend/src/components/DecisionList.tsx`
- `frontend/src/components/Results.tsx`
- `frontend/tests/scoring.test.ts`
- `.opencode-workflow-state.md`
**Files deleted**: None
**Validation performed/results**:
- `rtk python3 -m pytest tests/ -q` → 154 passed, 92 warnings.
- `rtk npm test` in `frontend/` → passed (`frontend scoring tests passed`).
- `rtk npm run typecheck` in `frontend/` → passed.
- `rtk npm run build` in `frontend/` → passed.
- `rtk python3 -m ruff check .` → not run successfully; Ruff is not installed in the active Python environment (`No module named ruff`).
**Known risks/blockers**:
- This is intentionally API- and DB-breaking. Existing local SQLite DBs should be deleted/reset because `create_all` will not drop `metrics.higher_is_better` or retrofit `activities.decision_id` constraints.
- Ruff validation remains environment-blocked until the dev dependency is installed.
**Next agent**: reviewer-gpt

## Implementer-gpt
**Status**: complete
**Phase**: implement
**Changes made**:
- `services/parser.py`: Removed runtime-unused legacy threshold parser helpers and switched parsing to `get_universal_criteria()`.
- `services/ontology.py`: Renamed `suggest_criteria(query)` to `get_universal_criteria()` with no compatibility alias.
- `tests/test_parser.py`: Removed tests that only covered deleted legacy threshold parser helpers.
- `tests/test_ontology.py`: Added coverage for `get_universal_criteria()` returning the full universal metric set.
- `routers/api.py`: Documented threshold request-boundary validation and persisted-threshold sanitization boundary.
- `services/scoring.py`: Removed redundant threshold value clamping after sanitization; retained score clamping for corrupt persisted `AlternativeScore` data.
- `models.py`: Added migration-deferred TODO for `Activity.decision_id` nullability/cascade cleanup.
- `requirements-dev.txt`: Added pinned Ruff dev dependency.
- `pyproject.toml`: Added conservative Ruff configuration.
- `frontend/package.json`: Added `typecheck` script using `tsc --noEmit`.
**Files created**:
- `pyproject.toml`
**Files modified**:
- `services/parser.py`
- `services/ontology.py`
- `tests/test_parser.py`
- `tests/test_ontology.py`
- `routers/api.py`
- `services/scoring.py`
- `models.py`
- `requirements-dev.txt`
- `frontend/package.json`
- `.opencode-workflow-state.md`
**Validation performed/results**:
- `python3 -m pytest tests/test_parser.py tests/test_ontology.py tests/test_scoring.py tests/test_routes.py -q` → 139 passed, 81 warnings.
- `npm run typecheck` in `frontend/` → passed.
- `python3 -m ruff check .` → not run successfully; Ruff is not installed in the current environment yet (`No module named ruff`).
**Deferred items**:
- Kept `Metric.higher_is_better` for schema/API compatibility.
- Kept `Activity.decision_id` nullable; non-null/cascade change remains migration-backed work.
**Next agent**: reviewer-gpt

## Reviewer-gpt
**Status**: approved
**Phase**: review-code
**Review notes**: Implementation matches the plan. Deleted threshold parser helpers and `suggest_criteria` have no remaining references. `get_universal_criteria()` rename is complete and clearer. Threshold validation still has request-boundary checks plus persisted-threshold sanitization; `filter_by_thresholds()` removed redundant threshold clamping while retaining score clamping. DB-breaking fields remain intact with comments only. Ruff and frontend typecheck additions are conservative. Tests preserve runtime coverage through route/scoring threshold paths and updated ontology/parser tests.
**Issues found**: None
**Required fixes**: None
**Next agent**: security-reviewer-gpt

## Security Reviewer-gpt
**Status**: changes-requested  
**Phase**: security-review  
**Findings**:
- **medium** — `routers/api.py:600-606` and `services/scoring.py:44-46`: `operator` is checked with `operator not in valid_operators` without first validating it is a string. If a request or persisted JSON supplies an unhashable value such as `[]` or `{}`, Python raises `TypeError`, causing a 500 response/crash instead of a controlled validation failure or sanitized skip.  
  **Remediation**: require `isinstance(operator, str)` before set membership. In the route, reject non-string operators with HTTP 422. In persisted-threshold sanitization, skip non-string operators.
- **info** — Deleted parser helpers / `get_universal_criteria()` rename: no remaining references to `suggest_criteria`, `extract_thresholds`, or `extract_thresholds_detailed` found.
- **info** — Added config/dependencies/scripts: `ruff==0.14.9` is pinned in `requirements-dev.txt`; `frontend/package.json` adds only `typecheck: "tsc --noEmit"`. No overly broad or obviously unsafe scripts identified.
- **info** — No hardcoded secrets or newly exposed sensitive files identified in reviewed changes. Existing `optium.db` is covered by `.gitignore`.

**Approved**: no  
**Required fixes**:
- Add string type validation for threshold `operator` at both request validation and persisted JSON sanitization boundaries.

**Next agent**: implementer-gpt

## Implementer-gpt
**Status**: complete
**Phase**: implement
**Changes made**:
- `routers/api.py`: Added request-boundary validation to reject non-string threshold `operator` values with HTTP 422 before valid-operator set membership.
- `services/scoring.py`: Added persisted-threshold sanitization to skip threshold entries with non-string `operator` values before valid-operator set membership.
- `tests/test_routes.py`: Added non-string `operator` cases for threshold API validation and persisted JSON sanitization/export paths.
**Files created**: None
**Files modified**:
- `routers/api.py`
- `services/scoring.py`
- `tests/test_routes.py`
- `.opencode-workflow-state.md`
**Validation performed/results**:
- `rtk python3 -m pytest tests/test_routes.py -q` → 73 passed, 70 warnings.
**Next agent**: reviewer-gpt

## Reviewer-gpt
**Status**: approved
**Phase**: review-code
**Review notes**: Re-review confirms the security finding was addressed correctly. API threshold validation now checks `operator` is a string before set membership, so list/dict operators return controlled 422 responses. Persisted threshold sanitization also skips non-string operators before valid-operator checks, avoiding `TypeError` on corrupt JSON. New route tests cover malformed request operators and persisted/export sanitization paths without weakening behavior. Earlier cleanup remains intact: deleted parser helpers/renamed universal criteria have no remaining references, and threshold validation layering remains clear.
**Issues found**: None
**Required fixes**: None
**Next agent**: security-reviewer-gpt

## Security Reviewer-gpt
**Status**: approved  
**Phase**: security-review  
**Findings**:
- **info** — `routers/api.py:606-608`: API threshold validation now checks `operator` is a string before valid-operator membership, so list/dict operators return controlled HTTP 422 responses instead of `TypeError`/500.
- **info** — `services/scoring.py:44-48`: persisted threshold sanitization now skips non-string operators before set membership. Result and export paths use sanitized thresholds via `routers/api.py:39-41` and `services/export.py:37-38`, so corrupt persisted operators are handled safely.
- **info** — Added tests/config/scripts/dependencies reviewed: malformed operator route/export/result tests cover the fix; `ruff==0.14.9`, conservative Ruff config, and `typecheck: "tsc --noEmit"` introduce no security concerns.
- **info** — No hardcoded secrets or newly exposed sensitive files identified. Existing local DB/venv patterns are ignored.

**Approved**: yes  
**Required fixes**: None  
**Next agent**: tester-gpt

## Tester-gpt
**Status**: passed  
**Phase**: test  
**Command run**: `rtk python3 -m pytest tests/ -q`  
**Output summary**: Backend test suite passed: **154 passed**, 92 warnings. No failures or environment blockers. (Frontend test/typecheck/build were not run because only one test command was executed in this pass.)  
**Failures relate to change**: n/a  
**Next agent**: linter-gpt

## Tester-gpt
**Status**: passed  
**Phase**: test  
**Command run**: `rtk npm test` ; `rtk npm run typecheck` ; `rtk npm run build` (all in `frontend/`)  
**Output summary**:  
- `rtk npm test` → passed (`frontend scoring tests passed`)  
- `rtk npm run typecheck` → passed  
- `rtk npm run build` → passed (`vite build` completed successfully)  
**Failures relate to change**: n/a  
**Next agent**: linter-gpt

## Linter-gpt
**Status**: failed  
**Phase**: lint  
**Command run**: `rtk python3 -m ruff check .`  
**Output summary**: Ruff could not run in the current environment: `/usr/bin/python3: No module named ruff`. `requirements-dev.txt` does now pin `ruff==0.14.9`, so the blocker is environment setup rather than missing project config.  
**Issues / blockers**: Backend static check not completed because Ruff is not installed in the active Python environment.  
**Next agent**: implementer-gpt

## Implementer-gpt
**Status**: complete
**Phase**: implement
**Changes made**: None; assessed Ruff setup and found no project source/config issue requiring changes.
**Files created**: None
**Files modified**:
- `.opencode-workflow-state.md`
**Validation performed/results**:
- Confirmed `requirements-dev.txt` already pins `ruff==0.14.9`.
- Confirmed `pyproject.toml` already contains conservative Ruff configuration.
- No source/config changes needed; lint blocker is the active Python environment missing Ruff (`No module named ruff`).
**Next agent**: linter-gpt
**Suggestion for linter-gpt**: Install dev requirements in the active environment (for example, `python3 -m pip install -r requirements-dev.txt`) and rerun `rtk python3 -m ruff check .`, or report lint as environment-blocked if dependency installation is outside scope.

## Linter-gpt
**Status**: failed
**Phase**: lint
**Command run**: `rtk python3 -m ruff check .`
**Output summary**: Backend Ruff check could not run: `/usr/bin/python3: No module named ruff`. An install attempt (`rtk python3 -m pip install -r requirements-dev.txt`) was blocked by the system Python’s externally-managed-environment policy (PEP 668), so this is environment-blocked rather than a code issue. Frontend static checks were already verified by Tester-gpt (`frontend` `npm run typecheck` passed).
**Issues / blockers**: Active Python environment does not have Ruff and cannot install dev requirements without changing the environment setup.
**Next agent**: commit-msg-gpt

## Commit Message-gpt
**Status**: complete
**Phase**: commit-msg
**Draft message**:
```md
refactor: simplify threshold validation and criteria API

- remove legacy threshold parser helpers and tests
- rename suggest_criteria to get_universal_criteria
- consolidate threshold validation and fix non-string operators
- defer DB-breaking removals with TODO comments
- add Ruff dev config, frontend typecheck script, and update tests
```
**Next agent**: (none - pipeline complete)

---

## Planner-gpt
**Status**: complete  
**Phase**: audit  

### Summary answer
Yes — there is still some leftover legacy/stale compatibility surface, but severity is **low to moderate**. The prior cleanup removed the obvious dead parser helpers. Remaining items are mostly **documentation drift**, **UI/API compatibility fields**, and **migration-backed schema leftovers**, not urgent runtime dead code.

### Safe cleanup now
- **Stale project/agent docs**
  - `AGENTS.md`
    - Lists `schemas.py`, but no such file exists.
    - Says `services/parser.py` does “legacy threshold extraction,” now removed.
    - Says “Screen decisions from older flows are still supported through legacy routes,” but no screen route was found.
    - Route table says `POST /api/decisions → parse question`; actual route is `POST /api/decide` in `routers/api.py`.
  - `.opencode-workflow-state.md` is a workflow artifact, not product code; already ignored.
- **Public UI still exposes internal mode language**
  - `frontend/src/components/Landing.tsx`: recent decision cards show `decision.mode`.
  - `frontend/src/components/DecisionList.tsx`: saved decisions show mode badges.
  - Product direction says no public mode picker; showing internal `choose/rank/diagnose` may be legacy-ish UI copy. Could hide, relabel, or map to friendlier “Comparison / Ranking / Diagnosis”.
- **Frontend compatibility shim likely removable after type confirmation**
  - `frontend/src/lib/scoring.ts`: comment references “older API responses”; `row.metric_id ?? metric?.id ?? 0` and `weighted_scores as any` suggest compatibility with older row shapes.
  - Backend currently includes `metric_id` in rows from `routers/api.py::_build_decision_detail`, so this can likely be tightened with frontend type updates/tests.

### Migration/deferred cleanup
- **`Metric.higher_is_better` remains throughout schema/API/frontend/tests**
  - `models.py::Metric.higher_is_better`
  - `main.py` metric seeding sync
  - `services/ontology.py::UNIVERSAL_DIMENSIONS`
  - `routers/api.py` responses: decision detail rows/metrics, refine criteria, `/api/metrics`
  - `frontend/src/types/index.ts`
  - Tests, especially `tests/test_scoring.py` legacy direction metadata cases
  - This is intentional compatibility for now; removal needs DB/API/frontend migration.
- **Nullable `Activity.decision_id` remains**
  - `models.py::Activity.decision_id` has TODO for non-null/cascade migration.
  - Keep deferred; requires DB audit for null/global activities and migration planning.
- **`Decision.mode` and `Decision.category` are still API-visible**
  - `models.py::Decision.mode/category`
  - `routers/api.py` list/detail/create responses
  - Frontend types and badges consume them.
  - `mode` still drives diagnosis-only dimension/gap output in `routers/api.py::_build_decision_detail`, so not dead. If removed/hidden, replace with derived flow or keep backend-only.

### Keep intentionally
- **Threshold persistence sanitization**
  - `services/scoring.py::sanitize_persisted_thresholds`
  - `routers/api.py::_parse_thresholds`
  - This is defensive compatibility for corrupt/older saved JSON and should stay.
- **Post-hoc threshold routes**
  - `/api/decisions/{id}/thresholds`
  - `/api/decisions/{id}/thresholds/clear`
  - These match current product flow, not legacy screen mode.
- **Robustness docs**
  - `docs/ROBUSTNESS.md` appears current and explicitly avoids old “significance/hypothesis testing” language.

### Suggested next steps
1. Do a docs-only cleanup of `AGENTS.md` route/file-tree/legacy wording.
2. Decide whether public UI should stop showing raw `mode` values; if yes, map/hide them in `Landing.tsx` and `DecisionList.tsx`.
3. Tighten `frontend/src/lib/scoring.ts` and `ScoreRow.metric_id` once confirmed all supported API responses include `metric_id`.
4. Create a separate migration task for `higher_is_better`, nullable `Activity.decision_id`, and possibly public `mode/category` API exposure.

**Next agent**: debater-gpt

## Debater-gpt
**Status**: approved-with-adjustments  
**Phase**: audit-review  

**Critique**: The audit findings are mostly real and prioritized appropriately. The remaining issues are low/moderate cleanup, not urgent runtime dead code. The AGENTS route claim is indeed stale: `routers/api.py` exposes `POST /api/decide`, not `POST /api/decisions`, and no legacy “screen” route was found. The `schemas.py` and parser “legacy threshold extraction” references are also stale.

**Corrections/adjustments**:
- Public `mode` badges in `Landing.tsx` and `DecisionList.tsx` are real, but should be framed as UX/copy cleanup rather than definite legacy debt. Showing inferred flow metadata can be acceptable; if kept, map raw values like `choose/rank/diagnose` to friendly labels.
- The frontend scoring shim should be cleaned cautiously. Backend detail rows currently include `metric_id`, but `ScoreRow.metric_id` is optional and the shim may protect tests/older cached responses. Tighten types and remove `as any` only with frontend tests/typecheck.
- Keep `Decision.mode` backend-side for now; it still affects diagnose-only dimension/gap output.
- Migration-deferred items (`higher_is_better`, nullable `Activity.decision_id`) are correctly classified.

**Next agent**: none

---

## Planner-gpt
**Status**: complete  
**Phase**: plan

### Summary
Plan a breaking cleanup that removes remaining legacy/older compatibility surfaces rather than preserving API/cached-response shims. Keep only defenses that protect current data integrity/security, not old response-shape compatibility.

### Implementation plan
1. **Clean stale docs**
   - Update `AGENTS.md`:
     - Remove nonexistent `schemas.py`.
     - Remove “legacy threshold extraction” wording for `parser.py`.
     - Remove “legacy routes” / backward compatibility claims.
     - Correct create route to actual `POST /api/decide`.
     - Reword `Decision.mode` as internal only if still retained.

2. **Remove public raw mode/category exposure**
   - Backend `routers/api.py`:
     - Remove `mode` from list/detail/create API responses where feasible.
     - Remove `category` from public responses unless the UI still needs a user-facing classification.
     - Keep `Decision.mode` internally only if needed for diagnose-specific dimension/gap output.
   - Frontend:
     - `Landing.tsx`, `DecisionList.tsx`: remove raw `decision.mode` badges.
     - If flow metadata is still desired, show friendly derived labels only, never raw `choose/rank/diagnose`.
     - Update `frontend/src/types/index.ts` to remove public `mode`/possibly `category`.

3. **Remove frontend scoring compatibility shim**
   - `frontend/src/lib/scoring.ts`:
     - Delete “older API responses” comment.
     - Require `metric_id` in score rows.
     - Remove `row.metric_id ?? metric?.id ?? 0`.
     - Remove `as any`.
   - Update TypeScript types so `ScoreRow.metric_id` is required.
   - Update frontend tests/fixtures to use current API shape only.

4. **Remove `Metric.higher_is_better` completely**
   - Backend:
     - Remove column from `models.py`.
     - Remove seed sync from `main.py`.
     - Remove field from `services/ontology.py`.
     - Remove field from API response builders in `routers/api.py`.
     - Update scoring/tests that reference metric direction metadata.
   - Frontend:
     - Remove `higher_is_better` from types and UI assumptions.
   - SQLite practical handling:
     - Because the repo uses `create_all` and no migrations, document that dev DBs must be reset after this breaking schema change, e.g. remove local `optium.db`.
     - Do not add runtime compatibility/migration shims for the old column.

5. **Make `Activity.decision_id` required and cascade-owned**
   - `models.py`:
     - Change `decision_id` to `nullable=False`.
     - Add `ForeignKey("decisions.id", ondelete="CASCADE")`.
     - Ensure ORM relationship cascade/delete behavior is correct.
   - Backend routes/tests:
     - Confirm deleting a decision removes activities, weights, scores, thresholds-related state.
   - SQLite handling:
     - Existing dev DBs with global/null activities must be reset or manually cleaned before use.

6. **Decision mode/category model decision**
   - Short term:
     - Keep `Decision.mode` internally only if needed for current result branching.
     - Remove from public API/types/export/UI.
   - Optional follow-up:
     - Replace stored `mode` with derived flow from alternatives count and score/result shape.
   - `Decision.category`:
     - Remove from public API if not actively user-facing.
     - Keep internally only if parser/scoring still needs it; otherwise remove in same schema-reset pass.

7. **Threshold persisted JSON sanitization**
   - Keep strict request validation.
   - Keep persisted JSON sanitization as a corruption/security guard, not legacy compatibility.
   - Reword comments to avoid “older/preexisting API” framing.
   - Do not support alternate old threshold shapes.

### Risks
- This is intentionally API- and DB-breaking.
- Existing local SQLite DBs may not match the new models because `create_all` will not drop columns or enforce new nullability on old tables.
- Frontend cached/local test fixtures may fail once compatibility fallbacks are removed.
- Removing public `mode` may require frontend result pages to rely on response contents rather than mode strings.

### Validation commands
- `rtk python3 -m pytest tests/ -q`
- `cd frontend && rtk npm test`
- `cd frontend && rtk npm run typecheck`
- `cd frontend && rtk npm run build`
- If Ruff is installed: `rtk python3 -m ruff check .`

**Next agent**: debater-gpt

## Debater-gpt
**Status**: changes-requested
**Phase**: review-plan
**Critique**: The plan is directionally correct for the user’s explicit “no older API/legacy compatibility” requirement, but it needs tightening before implementation. Removing public compatibility fields/shims is safe if done consistently across backend responses, frontend types, UI, exports, and tests; the current plan misses some public/export surfaces and leaves “where feasible” too vague. `Metric.higher_is_better` removal is feasible in this repo without migrations because tests create fresh schemas and the project uses `create_all`, but it must be treated as a reset-only breaking schema change: old SQLite DBs may retain extra columns or miss new constraints, so the implementation should not add compatibility shims and should document/delete/reset local DBs as needed. Making `Activity.decision_id` non-null with `ondelete="CASCADE"` is also feasible for fresh schemas, but existing DBs will not be altered by `create_all`; ORM cascade/delete tests should verify current behavior, and the reset requirement should be explicit. `Decision.mode` should be removed from public API/UI/export but retained internally for now because it still drives diagnose-only dimension/gap analysis. `Decision.category` should likewise be removed from public responses unless intentionally user-facing, but can remain internal if parser/activity defaults still use it. Threshold sanitization should remain, but comments/docstrings should frame it as corrupt persisted JSON/security/data-integrity handling, not older API compatibility.

**Required changes**:
- Make public `mode`/`category` removal explicit for **all** public surfaces, including `services/export.py` markdown/data (`Mode:` / `Category:` lines), not just `routers/api.py` and UI cards.
- Add impacted tests explicitly:
  - `tests/test_routes.py` `/api/decide`, list/detail, metrics, refine, delete/cascade, and export-markdown assertions.
  - `tests/test_ontology.py`, `tests/test_scoring.py`, and `tests/test_robustness.py` for `higher_is_better` fixture/model removal.
  - Frontend tests/fixtures under `frontend/tests` if they rely on optional `metric_id`, `mode`, `category`, or `higher_is_better`.
- Replace “remove `mode` from responses where feasible” with “remove from public JSON/export/types; keep only as internal DB/model field used for branching.”
- State that `Activity.decision_id` non-null/cascade and `Metric.higher_is_better` removal are implemented as breaking fresh-schema/reset changes, with no migration or runtime compatibility shim.
- Include `services/export.py`, `frontend/src/lib/api.ts`, and any docs with public legacy/mode wording such as `VISION.md` if touched/validated.

**Next agent**: planner-gpt

## Planner-gpt
**Status**: complete  
**Phase**: revised-plan

### Revised Implementation Plan

1. **Remove public `mode` / `category` from all public surfaces**
   - Keep `Decision.mode` in `models.py` **internally only** for current backend branching, especially diagnose-specific dimension/gap behavior.
   - Remove `mode` and `category` from public JSON responses in `routers/api.py`, including:
     - `POST /api/decide`
     - `GET /api/decisions`
     - `GET /api/decisions/{id}`
     - any helper/build response payloads used by these routes.
   - Remove `mode` and `category` from public export output in `services/export.py`, including markdown lines such as `Mode:` / `Category:` and any exported structured metadata.
   - Remove `mode` / `category` from frontend public types in `frontend/src/types/index.ts`.
   - Remove UI usage of raw flow metadata:
     - `frontend/src/components/Landing.tsx`
     - `frontend/src/components/DecisionList.tsx`
     - any result/review/scoring components that consume `decision.mode` or `decision.category`.
   - Do not replace with an older-API compatibility alias. If UI needs flow-specific rendering, derive from current response shape or add a non-public/internal backend branch only.

2. **Validate and update `frontend/src/lib/api.ts`**
   - Audit all API response parsing/normalization for assumptions about `mode`, `category`, `higher_is_better`, and optional legacy score-row shapes.
   - Remove any compatibility mapping that preserves old public fields.
   - Ensure TypeScript request/response helpers match the new public JSON contract exactly.
   - Confirm no API helper reintroduces `mode` / `category` into frontend-facing objects.

3. **Remove frontend scoring compatibility shim**
   - In `frontend/src/lib/scoring.ts`:
     - Remove comments referring to “older API responses.”
     - Require current score row shape only.
     - Make `metric_id` required in types/fixtures.
     - Remove fallback logic like `row.metric_id ?? metric?.id ?? 0`.
     - Remove `as any` casts tied to legacy response compatibility.
   - Update frontend fixtures/tests accordingly.

4. **Remove `Metric.higher_is_better` as a breaking fresh-schema/reset-only change**
   - No migration shim, runtime fallback, or compatibility layer.
   - Backend changes:
     - Remove `higher_is_better` column from `models.py::Metric`.
     - Remove `higher_is_better` from metric seed data in `services/ontology.py`.
     - Remove seed sync/update logic from `main.py`.
     - Remove field from `/api/metrics`, decision detail responses, refine responses, and any metric serialization in `routers/api.py`.
     - Update scoring/robustness logic and fixtures that reference direction metadata.
   - Frontend changes:
     - Remove `higher_is_better` from all TypeScript types, fixtures, API helpers, and UI assumptions.
   - Document/reset expectation:
     - Existing local SQLite DBs must be deleted/reset because `create_all` will not drop the old column.
     - Do not support old DB shape.

5. **Make `Activity.decision_id` non-null/cascade as a breaking fresh-schema/reset-only change**
   - No migration shim or compatibility layer for null/global activities.
   - In `models.py`:
     - Set `Activity.decision_id` to `nullable=False`.
     - Use `ForeignKey("decisions.id", ondelete="CASCADE")`.
     - Ensure ORM relationship cascade/delete-orphan behavior is correct.
   - Update route/delete behavior tests to verify deleting a decision removes owned activities/scores/weights as expected.
   - Document/reset expectation:
     - Existing local SQLite DBs with null activities must be deleted/reset.
     - Do not support old null/global activity rows.

6. **Keep threshold sanitization only as security/data-integrity guard**
   - Keep strict request validation in `routers/api.py`.
   - Keep persisted JSON sanitization in `services/scoring.py`.
   - Reword comments/docstrings so they describe protection against corrupt persisted JSON, malformed data, and security/data-integrity risks.
   - Do **not** frame sanitization as old API compatibility.
   - Do not support alternate legacy threshold shapes.

7. **Update docs and public wording**
   - Update `AGENTS.md`:
     - Remove nonexistent `schemas.py`.
     - Remove legacy threshold extraction wording.
     - Remove legacy route/backward compatibility claims.
     - Correct public create route wording to current `POST /api/decide`.
     - Describe `Decision.mode` as internal-only if mentioned.
   - Validate/update other docs with public legacy/mode wording, including:
     - `VISION.md`
     - `README.md` if present
     - docs under `docs/`
   - Do not document older API compatibility as supported.

8. **Full impacted tests list**
   - Backend:
     - `tests/test_routes.py`
       - `/api/decide` response no longer includes public `mode` / `category`.
       - list/detail responses no longer include public `mode` / `category`.
       - `/api/metrics` no longer includes `higher_is_better`.
       - refine response assertions updated for removed metric direction field.
       - delete/cascade behavior verifies activities/scores/weights are removed.
       - export-markdown assertions remove `Mode:` / `Category:`.
       - threshold sanitization tests remain, but expected comments/behavior should be security/data-integrity focused.
     - `tests/test_ontology.py`
       - remove `higher_is_better` expectations from universal metric fixtures.
     - `tests/test_scoring.py`
       - remove metric direction metadata assumptions.
     - `tests/test_robustness.py`
       - update fixtures/models for removed `higher_is_better`.
     - `tests/test_parser.py`
       - update if category/mode expectations leak into public response assumptions.
   - Frontend:
     - `frontend/tests/*`
       - update fixtures relying on optional `metric_id`, `mode`, `category`, or `higher_is_better`.
       - confirm API/type expectations match new public contract.
     - Typecheck should catch remaining references in:
       - `frontend/src/types/index.ts`
       - `frontend/src/lib/api.ts`
       - `frontend/src/lib/scoring.ts`
       - `frontend/src/components/*`

### Risks

- This is intentionally API- and DB-breaking.
- Existing SQLite DBs must be reset; `create_all` will not remove `higher_is_better` or retrofit non-null/cascade constraints.
- Removing public `mode` may require frontend pages to rely on response structure instead of explicit mode strings.
- Export consumers expecting `Mode:` / `Category:` lines will break.
- Removing frontend compatibility shims may expose stale fixtures or cached-response assumptions.

### Validation commands

- `rtk python3 -m pytest tests/ -q`
- `cd frontend && rtk npm test`
- `cd frontend && rtk npm run typecheck`
- `cd frontend && rtk npm run build`
- If Ruff is available: `rtk python3 -m ruff check .`

**Next agent**: debater-gpt

## Debater-gpt
**Status**: approved
**Phase**: review-plan
**Critique**: The revised plan addresses my required changes. It explicitly removes public `mode`/`category` from JSON, export, frontend types, and UI surfaces; includes `services/export.py`, `frontend/src/lib/api.ts`, and docs validation; treats `Metric.higher_is_better` and `Activity.decision_id` changes as intentional breaking fresh-schema/reset-only changes with no migration or compatibility shim; and lists the impacted backend/frontend tests. Keeping `Decision.mode` internally for current branching and threshold sanitization as a security/data-integrity guard is appropriate and does not conflict with the user’s no-legacy-compatibility requirement.
**Required changes**: None
**Next agent**: implementer-gpt

## Implementer-gpt
**Status**: complete
**Phase**: implement
**Changes made**:
- `models.py`: Removed `Metric.higher_is_better`; made `Activity.decision_id` non-null with `ondelete="CASCADE"`.
- `routers/api.py`: Removed public `mode`, `category`, and `higher_is_better` from JSON responses; kept `Decision.mode` internal for branching.
- `services/export.py`: Removed `mode`/`category` from markdown export data/output.
- `frontend/src/types/index.ts`: Removed public removed fields and made `ScoreRow.metric_id` required.
- `frontend/src/lib/api.ts`: Validated; no compatibility mapping or removed-field preservation was present.
- `frontend/src/lib/scoring.ts`: Removed old response-shape fallback and compatibility `as any`.
- Frontend UI/tests and backend tests updated accordingly.
- Docs updated to remove stale legacy wording and note local DB reset expectations.

**Files created**: None

**Files modified**:
- `AGENTS.md`, `README.md`, `VISION.md`
- `models.py`, `main.py`, `routers/api.py`
- `services/export.py`, `services/ontology.py`
- `tests/test_ontology.py`, `tests/test_robustness.py`, `tests/test_routes.py`, `tests/test_scoring.py`
- `frontend/src/types/index.ts`, `frontend/src/lib/scoring.ts`
- `frontend/src/components/Landing.tsx`, `frontend/src/components/DecisionList.tsx`, `frontend/src/components/Results.tsx`
- `frontend/tests/scoring.test.ts`
- `.opencode-workflow-state.md`

**Validation performed/results**:
- `rtk python3 -m pytest tests/ -q` → 154 passed, 92 warnings.
- `rtk npm test` in `frontend/` → passed.
- `rtk npm run typecheck` in `frontend/` → passed.
- `rtk npm run build` in `frontend/` → passed.
- `rtk python3 -m ruff check .` → blocked; Ruff not installed (`No module named ruff`).

**Known risks/blockers**:
- Intentional API/DB breaking change. Existing local SQLite DBs should be reset because `create_all` will not drop old columns or retrofit constraints.
- Ruff remains environment-blocked until dev dependencies are installed.

**Next agent**: reviewer-gpt

## Reviewer-gpt
**Status**: changes-requested
**Phase**: review-code
**Review notes**: The cleanup is largely consistent with the approved no-legacy plan. Public decision `mode`/`category` were removed from API response payloads, export data/markdown, frontend decision types, and UI usage while `Decision.mode` remains internal for diagnose branching. `Metric.higher_is_better` is removed from the model, ontology seed data, API serialization, frontend types/usages, and tests. `frontend/src/lib/scoring.ts` now requires `metric_id` and no longer has old response-shape fallback or `as any`; `frontend/src/lib/api.ts` does not preserve removed fields. Threshold sanitization comments are framed around malformed/corrupt persisted data rather than old API compatibility. Docs touched appear aligned with the new public flow.

**Issues found**:
- Missing required test coverage for delete/cascade behavior. The plan explicitly requires route/delete behavior tests verifying that deleting a decision removes owned activities, weights, and scores. Current `tests/test_routes.py` has no delete/cascade test for `POST /api/decisions/{id}/delete` and no assertion that `Activity`, `DecisionWeight`, and `AlternativeScore` rows are cleaned up.

**Required fixes**:
- Add backend route coverage for decision deletion/cascade ownership. At minimum, seed a scored decision with activities, weights, and alternative scores, call `POST /api/decisions/{id}/delete`, then assert:
  - response is `{"status": "deleted"}`;
  - the `Decision` row is gone;
  - owned `Activity` rows are gone;
  - owned `DecisionWeight` rows are gone;
  - related `AlternativeScore` rows are gone.

**Next agent**: implementer-gpt

## Implementer-gpt
**Status**: complete
**Phase**: implement
**Changes made**:
- `tests/test_routes.py`: Added route coverage for `POST /api/decisions/{id}/delete` using a scored decision with activities, decision weights, and alternative scores; asserts the delete response and removal of the `Decision`, owned `Activity`, owned `DecisionWeight`, and related `AlternativeScore` rows.
**Files created**: None
**Files modified**:
- `tests/test_routes.py`
- `.opencode-workflow-state.md`
**Validation performed/results**:
- `rtk python3 -m pytest tests/test_routes.py::test_delete_decision_removes_owned_rows -q` → 1 passed, 2 warnings.
**Next agent**: reviewer-gpt

## Implementer-gpt
**Status**: complete  
**Phase**: implement  

**Changes made**:
- `tests/test_routes.py`: Added route coverage for `POST /api/decisions/{id}/delete` using a scored decision with activities, decision weights, and alternative scores; asserts the delete response and removal of `Decision`, owned `Activity`, owned `DecisionWeight`, and related `AlternativeScore` rows.

**Files created**: None

**Files modified**:
- `tests/test_routes.py`
- `.opencode-workflow-state.md`

**Validation performed/results**:
- `rtk python3 -m pytest tests/test_routes.py::test_delete_decision_removes_owned_rows -q` → 1 passed, 2 warnings.

**Next agent**: reviewer-gpt

## Reviewer-gpt
**Status**: approved
**Phase**: review-code
**Review notes**: Re-review confirms the cascade delete test was added correctly. `test_delete_decision_removes_owned_rows` seeds a scored decision with all related models, calls `POST /api/decisions/{id}/delete`, asserts the 200/deleted response, and checks removal of all four model types. No legacy compatibility fields (`higher_is_better`, `mode`, `category`) are reintroduced to public response assertions, API response payloads, or export surfaces. The delete route also correctly cleans up AlternativeScore rows manually before ORM deletion.
**Issues found**: None
**Required fixes**: None
**Next agent**: security-reviewer-gpt

## Security Reviewer-gpt
**Status**: approved
**Phase**: security-review

**Findings**:

- **info** — Delete route manual AlternativeScore cleanup is correct but fragile. Consider adding ORM cascade from Activity → AlternativeScore as defense-in-depth.
- **info** — `Metric.higher_is_better` removal is consistent across all layers.
- **info** — `Activity.decision_id` non-null + cascade is safe for all current codepaths.
- **info** — Public mode/category removal introduces no security concerns.
- **info** — Frontend scoring shim removal is safe.
- **info** — Threshold sanitization is properly framed as a security/data-integrity guard.
- **info** — No new injection vectors identified.

**Approved**: yes
**Required fixes**: None
**Next agent**: tester-gpt

## Tester-gpt
**Status**: passed  
**Phase**: test  
**Commands run and results**:
- `python3 -m pytest tests/ -q` → 155 passed, 93 warnings ✅
- `npm test` in `frontend/` → passed ✅
- `npm run typecheck` in `frontend/` → passed ✅
- `npm run build` in `frontend/` → passed ✅
**Notable**: Backend test count increased from 154 to 155 (new cascade delete test).
**Next agent**: linter-gpt

## Linter-gpt
**Status**: passed
**Phase**: lint
**Command run**: `python3 -m ruff check .` (via `.venv/bin/python`)
**Output summary**: Backend: **All checks passed!** Ruff found zero issues. Frontend typecheck was already confirmed by Tester-gpt (passed). No code fixes needed.
**Next agent**: commit-msg-gpt

## Commit Message
**Status**: complete
**Phase**: commit-msg
**Draft message**:
```
refactor: strip legacy compatibility surfaces from API, DB, and frontend

- Remove public mode/category from API responses, export, frontend
  types, and UI badges (keep Decision.mode internal for branching)
- Drop Metric.higher_is_better column (breaking schema — reset local DB)
- Make Activity.decision_id non-null with ondelete="CASCADE"
  (breaking schema — reset local DB)
- Remove frontend scoring compatibility shim: legacy metric_id fallback,
  as any cast, and unused Metric parameter from recomputeFitScores
- Add cascade delete test for decision removal
- Update AGENTS.md, README.md, VISION.md with current route/docs
- Resolve all Ruff lint issues (zero findings)
```
**Next agent**: (none - pipeline complete)

---

## Implementer
**Status**: complete
**Phase**: implement
**Changes made**:
- `tests/test_scoring.py`: Removed unused import `sanitize_persisted_ko_criteria` from the services.scoring import line; removed unused variable `alt` assignment in `test_ko_missing_score` (changed `alt = make_activity(...)` to bare `make_activity(...)`)
**Files created**: None
**Files modified**:
- `tests/test_scoring.py`
**Validation performed/results**:
- `rtk .venv/bin/python -m ruff check .` → All checks passed!
- `rtk python3 -m pytest tests/test_scoring.py -q` → 23 passed
**Next agent**: reviewer


---

## Planner-gpt (KO Criteria)

**Status**: complete  
**Phase**: plan  

### Summary
Plan for implementing KO (Knock-Out) criteria as a pre-scoring eligibility gate. KO criteria are hard-requirement thresholds on metrics that alternatives must pass before being ranked. They are stored as a separate JSON column (`Decision.ko_criteria`) independent from the existing post-hoc threshold system. User configures KO during Review, scores are evaluated after submission, and only KO-eligible alternatives appear in ranking.

### Key decisions
- KO and existing post-hoc thresholds **coexist independently** (KO = pre-scoring gate, thresholds = post-scoring filter)
- **Two-status model**: `"passed"` and `"knocked_out"` only (no "unknown"; missing scores → knocked_out)
- KO configured only via refine endpoint (no dedicated KO route)
- KO stored as JSON on `Decision`, same pattern as thresholds
- Backend filters `results`/`series`/`robustness` to KO-eligible alternatives automatically

**Next agent**: debater

## Debater-gpt (KO Criteria)

**Status**: approved  
**Phase**: review-plan  

### Critique
Plan is well-designed and addresses core requirements cleanly. Two-status model, both-or-neither validation, defensive sanitization, and independent coexistence with thresholds are all sound. Edge cases properly handled. Security hardening (isinstance before set membership) consistent with prior threshold work.

**Required changes**: None.

**Next agent**: implementer

## Implementer-gpt (KO Criteria)

**Status**: complete  
**Phase**: implement  

### Changes made
- `models.py`: Added `ko_criteria` JSON column to `Decision` model
- `services/scoring.py`: Added `sanitize_persisted_ko_criteria()` and `evaluate_ko_criteria()` functions
- `routers/api.py`: Added KO validation to refine endpoint (operator/value checks, both-or-neither rule, security hardening); updated `_build_decision_detail()` to evaluate KO and filter results/series/robustness; added `_parse_ko_criteria()` helper
- `services/export.py`: Added KO criteria/results to export data and markdown brief (section after Ranking, before Threshold Filters)
- `frontend/src/types/index.ts`: Added `KoCriterion`, `KoResultEntry`, `KoResult` interfaces; updated `DecisionDetail`; updated `RefinePayload`
- `frontend/src/components/Review.tsx`: Added "Knock-Out Criteria" collapsible Card section with per-metric toggle/operator/value controls
- `frontend/src/components/Results.tsx`: Added KO evaluation card with PASS/KNOCKED OUT status badges; ranking filtered to eligible alternatives
- `tests/test_scoring.py`: Added `TestEvaluateKoCriteria` (6 tests)
- `tests/test_routes.py`: Added `TestKoApi` (11 tests)

### Files modified
- `models.py`
- `services/scoring.py`
- `routers/api.py`
- `services/export.py`
- `frontend/src/types/index.ts`
- `frontend/src/components/Review.tsx`
- `frontend/src/components/Results.tsx`
- `tests/test_scoring.py`
- `tests/test_routes.py`

### Validation performed/results
- `rtk python3 -m pytest tests/ -q` → 174 passed, 112 warnings
- `rtk npm test` in `frontend/` → frontend scoring tests passed
- `rtk npm run typecheck` in `frontend/` → passed
- `rtk npm run build` in `frontend/` → build succeeded
- `rtk .venv/bin/python -m ruff check .` → All checks passed

**Next agent**: reviewer

## Reviewer-gpt (KO Criteria)

**Status**: approved  
**Phase**: review-code  

### Review notes
KO criteria feature is well-designed and implemented across all layers. Proper separation from threshold system (separate JSON column, dedicated sanitizer/evaluator, independent API helpers). Validation is thorough (both-or-neither rule, isinstance checks before set membership, range/finite checks). Edge cases handled (missing scores, boundary scores, NaN/Inf clamping, all knocked out). Frontend integration clean (KO toggle in Review, KO card in Results). Existing threshold system completely untouched. Tests comprehensive.

### Issues found
- Minor: `RefineResponse` missing `ko_criteria` type; missing route test for corrupt persisted KO data; potential KO+threshold display overlap in Results threshold panel
- No required fixes

**Next agent**: security-reviewer

## Security Reviewer-gpt (KO Criteria)

**Status**: approved  
**Phase**: security-review  

### Findings
- **info** — Empty list `[]` in `ko_criteria_input` silently clears persisted KO; document behavior
- **info** — TOCTOU window between `_parse_ko_criteria()` and `evaluate_ko_criteria()` (theoretically possible but SQLite single-writer prevents in practice)
- **info** — Threshold filtering overwrites robustness after KO filtering (logical inconsistency, not security)
- **info** — No DB-level CHECK constraint on `ko_criteria` (mitigated by app-layer sanitization)

No SQL injection, JSON injection, code injection, XSS, path traversal, type confusion, secrets exposure, or DoS vectors found. All three defensive layers (request validation, persisted-data sanitization, evaluation) are properly implemented.

**Required fixes**: None.

**Next agent**: tester

## Tester-gpt (KO Criteria)

**Status**: passed  
**Phase**: test  

### Commands run and results
- `python3 -m pytest tests/ -q` → 174 passed, 112 warnings ✅
- `npm test` in `frontend/` → passed ✅
- `npm run typecheck` in `frontend/` → passed ✅
- `npm run build` in `frontend/` → passed ✅

**Next agent**: linter

## Linter-gpt (KO Criteria)

**Status**: passed  
**Phase**: lint  

### Command run
- `.venv/bin/python -m ruff check .`

### Output summary
Backend: **All checks passed!** Zero ruff findings. Frontend typecheck was already confirmed by Tester (passed). No code fixes needed.

**Next agent**: commit-msg

## Commit Message-gpt (KO Criteria)

**Status**: complete  
**Phase**: commit-msg  

### Draft message
```
feat: add knock-out (KO) criteria for filtering alternatives by score thresholds

Introduce optional knock-out criteria on decisions, enabling users to set
per-metric minimum/maximum thresholds that eliminate ineligible alternatives
from results.

- Add `Decision.ko_criteria` JSON column for persisting KO rule definitions
- Implement `sanitize_persisted_ko_criteria()` and `evaluate_ko_criteria()`
  in the scoring service
- Validate KO criteria on refine (operator, value 0-100, metric membership)
- Filter results, radar series, and robustness to KO-eligible alternatives
- Include KO criteria and results in markdown export briefs
- Add TypeScript interfaces: `KoCriterion`, `KoResultEntry`, `KoResult`
- Render KO configuration UI on Review page (enabled toggle, operator
  select, value input per metric)
- Display KO evaluation card on Results page with PASSED/KNOCKED OUT
  statuses and ranked survivors
- Add comprehensive tests for KO evaluation logic and API endpoints
```

**Next agent**: (none - pipeline complete)
